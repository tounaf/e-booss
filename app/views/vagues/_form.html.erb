<%= form_with(model: vague, local: true) do |form| %>
  <% if vague.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vague.errors.count, "error") %> prohibited this vague from being saved:</h2>

      <ul>
      <% vague.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rentree %>
    <%= form.date_select :rentree %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
