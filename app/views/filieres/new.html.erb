<div class="container">
    <%= form_for @filiere, url: filieres_path  do |f| %>

        <fieldset class="form-group">
            <label for="nom">Nom</label>
            <%= f.text_field :nom %>
        </fieldset>

        <%= f.submit "Ajouter", class: "btn btn-primary" %>
    <% end %>
    <div><a href="<%= filieres_path %>">Voir tous les filières</a></div>
</div>
