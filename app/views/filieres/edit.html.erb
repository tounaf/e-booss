<h1>Filieres#edit</h1>
<div class="container">
    <%= form_for @filiere do |f| %>

        <fieldset class="form-group">
            <label for="nom">Nom</label>
            <%= f.text_field :nom %>
        </fieldset>

        <%= f.submit "Modifier", class: "btn btn-primary" %>
    <% end %>
    <div><a href="<%= filieres_path %>">Voir tous les filières</a></div>
</div>
